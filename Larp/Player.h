#include <iostream>

class Player {
    private:
        std::string Name;
        int Health;
        int Level;
        int Gold;
    
    public:
        //inventory
        //enum equipped weapon
        //

    Player(std::string name, int health, int level, int gold) :
    Name(name),Health(health),Level(level), Gold(gold) {};

    
};